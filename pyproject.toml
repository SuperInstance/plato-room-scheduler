[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-room-scheduler"
version = "0.1.0"
description = "Room scheduler — temperature-based training scheduling"
license = {text = "MIT"}
requires-python = ">=3.8"
